# Real secrets live here - copy config.example.h to config.h and fill in
# your own WiFi/OTA/MQTT credentials. Never commit config.h itself.
config.h

# Arduino/IDE build artifacts, if ever built inside this folder
build/
*.bin
*.elf
*.map
