# Real secrets live here - copy src/config.example.h to src/config.h and
# fill in your own WiFi/OTA/MQTT credentials. Never commit config.h itself.
src/config.h

# PlatformIO / Arduino build artifacts
.pio/
build/
*.bin
*.elf
*.map
